# zNES の現在地

Zig で書かれた NES エミュレータ。デスクトップ（SDL3）とブラウザ（WebAssembly）の
2つのビルドがあり、どちらも同じエミュレーションコアを動かす。

この文書は、いま何ができて何ができないかを一覧にしたもの。

## 対象とする機械

NTSC 版の NES / ファミリーコンピュータ1台。それ以外の機械は対象外で、
将来的にも対応する予定はない。具体的には以下を含まない。

- PAL 版と Dendy
- チップのリビジョン違い（NEC 版 MMC3、初期リビジョンの 2A03 など）
- アーケード用の PPU（2C03 / 2C04 / 2C05）を積んだ Vs. System や PlayChoice-10

## 動かし方

- `zig build run -- <ROM ファイル>` でデスクトップ版が起動する。
  引数なしで起動して、あとから ROM をドラッグ＆ドロップしてもよい。
- `zig build wasm` でウェブ版を `zig-out/web` に出力する。
- `zig build serve` でウェブ版をビルドして HTTP で配信する。
  ブラウザで開き、ROM をドラッグ＆ドロップするか、ボタンから選ぶ。
- `zig build test` で単体テストを実行する。
- `zig build test-full` で単体テストに加えて ROM を使った統合テストを実行する。
  数分かかる。

## 操作

- 方向キー: 十字キー
- Z: A ボタン
- X: B ボタン
- Enter: START
- 右 Shift: SELECT
- Ctrl+R（macOS では Cmd+R）: リセット
- Ctrl+Shift+R: 電源の入れ直し
- Ctrl+Z（macOS では Cmd+Z）: ポート2をコントローラと Zapper とで切り替える
- マウス: Zapper の照準。左ボタンが引き金
- ゲームパッド: 接続すると先着2台が2つのポートを担当する

ウェブ版はキー割り当てが同じで、リセットは R、電源の入れ直しは Shift+R。
ゲームパッドは使えるが、Zapper の照準はマウスまたはタッチになる。

## 精度

- AccuracyCoin: 141 項目すべて通過。
- ROM を使った統合テスト: 36 のテストグループがすべて期待どおりの結果を返す。
  1つのグループが複数の ROM を実行するので、ROM の本数はこれより多い。
  リポジトリには 264 本のテスト ROM が置いてある。
- 統合テストのうち3本は、意図的に不合格の結果を固定してある。
  `mmc3_test/6-MMC6`、`mmc3_test_2/6-MMC3_alt`、`mmc3_irq_tests/5.MMC3_rev_A` の3本で、
  理由はいずれも同じ。MMC3 の IRQ カウンタには2つのリビジョンがあり、
  片方を通すともう片方が必ず落ちる。znes は Sharp 版（MMC3B / MMC3C）を実装しているので、
  NEC 版を要求するこの3本は通らない。これは未実装ではなく、選択の結果である。
- テストが通ることと実機と一致することは同じではない。
  テストが触れていない領域は残っており、詳細は `known_gaps.md` にある。

## 対応しているカートリッジ

iNES のマッパー番号で14種類、基板の種類でいうと16種類。

- 0: NROM
- 1: MMC1（SNROM / SOROM / SZROM / SXROM）
- 2: UxROM
- 3: CNROM
- 4: MMC3（TxROM）
- 4 のサブマッパー1: MMC6（HKROM）
- 7: AxROM
- 9: MMC2（PxROM）
- 10: MMC4（FxROM）
- 11: Color Dreams
- 13: CPROM
- 34: BNROM と NINA-001（番号を共有する無関係な2種類の基板）
- 66: GxROM（GNROM / MHROM）
- 119: TQROM
- 206: Namco 108（DxROM）

これ以外のマッパー番号を持つ ROM は読み込み自体を拒否する。
北米でライセンスされたタイトルの一覧（781 本、うち基板が判明しているものが 680 本）と
突き合わせると、632 本（判明分の約 93 パーセント）がこの一覧の基板を使っている。

対応していないものと、その理由は次のとおり。

- Konami VRC 系（21 / 22 / 23 / 24 / 25 / 26 など）: IRQ カウンタの系統が別で、
  VRC6 と VRC7 は音源も追加される。
- MMC5: 複雑さが他と比べて桁違いに大きい。
- 海賊版・多本数収録カートリッジ・互換機専用のマッパー: ライセンス製品ではない。
- 118（TxSROM）: MMC3 の一種だが、ネームテーブルをページ単位で選ぶ配線を持つ。
  現在のミラーリングの表現方法では表せず、変更するとすべてのマッパーに波及する。
- 99（Vs. System）: 対象とする機械の外側。

## 対応している入力機器

- 標準コントローラ2個（2ポート）
- Zapper（光線銃）

Zapper は自動では挿さらない。カートリッジ側に「光線銃を使う」と書く場所がないため、
キー操作でポート2に挿す。挿していなければ何も起きない。

以下は対応していない。任天堂製で、NES とファミコンの両方に存在し、
論理的に同じ機器であること、という線を引いた結果である。

- Four Score / 拡張コントローラによる3人以上の同時プレイ
- Power Pad（ファミリートレーナー）: 見た目は同じだが通信方式が別物
- アルカノイドコントローラ、パワーグローブなどの他社製品
- NES Satellite

## セーブ

バッテリー付きカートリッジのセーブに対応している。セーブステートはない。

- 保存先はデスクトップ版が OS のアプリケーションデータ領域。
  macOS では `~/Library/Application Support/znes/znes/<ROM のファイル名>.sav` になる。
- ウェブ版は `localStorage` に `znes.sav:<ROM のファイル名>` というキーで保存する。
  内容は base64 で符号化してある。
- カートリッジの RAM が最初に書き換えられてから約1秒後に書き出す。
  その後さらに書き換えがあっても先送りはしないので、1秒以内に必ず保存される。
  ROM を入れ替えるときと終了するときにも書き出す。
- セーブの先頭には、どのカートリッジのものかを示す 33 バイトの情報が入っている。
  置き場所の名前は ROM のファイル名だが、そこにあるものが誰のセーブかは
  この情報で判断する。
- ROM のファイル名を変えてもセーブは付いてくる。
  新しい名前の場所に無ければ、保存済みのセーブを順に見て一致するものを探し、
  見つかったら新しい名前へ移す。
- 名前が同じで中身の違う ROM があっても、互いのセーブを読み書きしない。
  先にあったセーブは上書きせず、別の名前に退避する。
  退避したセーブも、元の ROM を読み込めば見つかって戻ってくる。
- デスクトップ版の書き出しは一時ファイルへ書き終えてから置き換える。
  書き込みの途中で異常終了しても、それまでのセーブは失われない。
- ROM を読み込んだ時点のセーブを1世代だけ残す。
  デスクトップ版は `<ROM のファイル名>.sav.bak`、
  ウェブ版は `localStorage` の `znes.sav.bak:<ROM のファイル名>` に置く。
  自動では読み戻さないので、必要なときは手で元のキーに書き戻すこと。

既知の制限は次のとおり。

- ROM に手を加えると別のカートリッジとして扱う。
  パッチを当てた ROM は新しいセーブから始まる。
  元のセーブは消さずに退避するので、手で戻すことはできる。
- ヘッダを持たない古い版のセーブは、ファイル名が一致する場合にのみ読み込む。
  一度読み込めばヘッダが付き、以降はリネームしても付いてくる。
- CHR RAM のバッテリーバックアップは保存しない。
  該当するのは非対応のマッパー2種類だけなので、実際には影響しない。

## ムービーの再生

記録済みの入力を再生できる。記録はできない。

- FCEUX の `.fm2`、および同じ入力ログを持つ `.fm3`
- BizHawk の `.bk2`

電源投入時から始まるムービーだけを受け付ける。
セーブステートから再開するムービーは、誤った状態から再生するくらいならと拒否する。

Zapper を使うムービーは読み込めない。ポートに座標と引き金をどう並べるかが
判明していないため、推測で実装すると黙って違う場所を撃つことになる。

再生が実機やほかのエミュレータとずれる場合、それは再現精度の差がそのまま出たもので、
再生機能の不具合とは限らない。

## 意図的に実装していないもの

以下はいずれも、実装できるかどうかではなく、実装しないと決めたものである。

- PAL と Dendy
- チップのリビジョン違い
- RGB PPU、および Vs. System
  （PlayChoice-10 の ROM は読み込めて動作するが、色が実機と異なる）
- OAM の DRAM としての減衰
- パレットの破損
- ドット未満の位相に依存する PPU のグリッチ
- CPU と PPU の位相関係4種類の切り替え。1種類に固定してある
- ROM の内容による識別、およびそのためのデータベース
- セーブステート

## 検証の弱いところ

- 対応している16種類の基板のうち、合否を報告するテスト ROM があるのは
  NROM、MMC1、CNROM、MMC3 の4種類だけである。
  残る12種類は単体テストと、起動して画面が出ることの確認しかしていない。
- Zapper を検証するテスト ROM がリポジトリにない。単体テストのみで確かめている。
- 光センサの反応時間は実測3点に最小二乗で当てはめたもので、
  3点のうち1点は1スキャンラインずれている。
- 電源投入直後のメモリの内容はすべて 0 で固定してある。
  実機は不定なので、未初期化メモリに依存する挙動は再現しない。
